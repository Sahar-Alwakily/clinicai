services:
  - type: web
    name: clinicai
    env: node
    buildCommand: rm -rf node_modules package-lock.json && npm install --legacy-peer-deps --no-audit --no-fund && npm run build
    startCommand: npm start
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production

